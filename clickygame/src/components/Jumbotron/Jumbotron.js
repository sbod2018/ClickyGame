import React from "react";

const Jumbotron = () => (
    <div className="jumbotron jumbotron-fluid">
    <div className="container text-center">
    <h1 className="display-4">Seinfeld Clicky Game</h1>
    <p className="lead">Click on an image to earn points, but not more than once, or else</p>
    </div>
    </div>
);

export default Jumbotron;